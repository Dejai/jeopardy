NEXT:
    For anything that needs to be hidden based on the config options
        > Set a common class of "optionsBased"
        > Then couple that with the thing itself (e.g. section for current player)
        > Then have CSS that says if a certain combo, then it should be hidden
            (e.g. PlayerSection.optionsBased.3 = hide )

    Don't forget to double check the combination of the different settings